[workspace]
members = ["tree-sitter-all"]
resolver = "2"

[profile.release]
opt-level = "s"        # Optimize for size (87+ grammars produce large binaries)
lto = false
strip = "debuginfo"    # Keep function symbols (needed for FFI), strip debug info only
